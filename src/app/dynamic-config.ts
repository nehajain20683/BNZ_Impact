// src/app/dynamic-config.ts
// Import this in any server component page that uses Prisma
// to prevent static generation errors at build time
export const dynamic    = 'force-dynamic';
export const revalidate = 0;
